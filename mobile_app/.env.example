# Copy to .env and set your PC's LAN IP (not localhost) when testing on a physical phone.
# Find IP: Windows `ipconfig`, macOS/Linux `ifconfig` or `ip addr`
EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api
