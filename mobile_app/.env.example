# Recommended for local dev (Expo Go / emulator):
EXPO_PUBLIC_API_URL=auto

# Physical device with fixed IP (run npm run env:api to generate):
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5000/api

# Android emulator only (if auto does not work):
# EXPO_PUBLIC_API_URL=http://10.0.2.2:5000/api
